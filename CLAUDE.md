@AGENTS.md

# 몸친 — 작업 규칙

비개발자가 만드는 앱이다. 설명은 한국어로, 무엇을 왜 했는지까지 말한다.

## 스택과 관례

- Next.js 16 App Router · TypeScript strict · Tailwind v4 · zod
- **화면에 보이는 글은 한국어. 코드 주석과 커밋 메시지도 한국어.**
  주석은 "무엇을"이 아니라 **"왜"** 를 적는다. 코드를 읽으면 아는 것은 쓰지 않는다.
- 모델 호출은 전부 `lib/claude.ts` 한 곳을 지난다. 모델·effort·오류 문구가 여기 모여 있어
  공급자나 모델을 바꿀 때 한 파일만 고치면 된다. 라우트에서 SDK를 직접 부르지 않는다.
- **숫자는 AI에게 맡기지 않는다.** 열량(`lib/nutrition.ts`), 오늘 운동량(`lib/activity.ts`),
  종목별 소모 열량(`lib/exercise.ts`)은 순수 수식이다. 같은 입력에 항상 같은 답이 나와야 한다.
  모델에게는 글쓰기와 판독만 맡긴다.
- 응답 모양은 zod 스키마로 강제한다(`lib/schema.ts`). 모양이 어긋나면 화면이 깨지기 전에 잡힌다.

## 환경변수와 키

- 키는 전부 `.env.local`에 둔다. git에 올리지 않는다(`.gitignore`에 `.env*`).
- **`.env.local`을 고쳤으면 개발 서버를 껐다 켠다.** Next.js는 부팅할 때 값을 읽어두기 때문에,
  키를 바꿔도 서버가 옛 값을 계속 쓴다. 이걸로 두 번 헤맸다.
- 새 키를 코드에 엮기 전에 `scripts/doctor.sh`로 **먼저 살아 있는지 확인한다.**
  키가 placeholder거나 크레딧이 없으면 기능을 다 만든 뒤에야 알게 된다.
- 배포에도 같은 이름으로 넣어야 한다. Vercel Settings → Environment Variables.
  로컬에만 넣으면 배포는 성공하고 API 호출만 500이 난다.

| 변수 | 쓰는 곳 |
|---|---|
| `ANTHROPIC_API_KEY` | 모든 모델 호출 |
| `KAKAO_REST_KEY` | 근처 식당 검색 + 메뉴 사진 검색 |
| `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` | 기기 간 백업 (`/api/backup`) |
| `ANTHROPIC_MODEL` | (선택) 모델 바꿀 때만 |

## 사람이 해야 하는 일은 먼저 말한다

계정 가입, 키 발급, OAuth 앱 설치, CLI 로그인처럼 **내가 대신 못 하는 일**은
코드를 쓰기 전에 번호 붙인 목록으로 먼저 알린다. 주소와 붙여넣을 위치까지 적는다.
기능을 다 만든 뒤에 "그런데 키가 필요합니다"라고 하면 그 시간이 다 낭비된다.

지금까지 걸렸던 것들: Vercel의 GitHub 로그인 연결, Vercel GitHub App 설치,
Supabase CLI 로그인, API 키 발급.

## 확인

- 기능 하나를 만들면 **바로 실제 API로 한 번 부르고 결과를 붙여넣는다.** 마지막에 몰아서 하지 않는다.
  실패하면 코드를 보기 전에 개발 서버부터 다시 켜서 옛 키 문제인지 먼저 지운다.
- 화면이 바뀌면 브라우저로 직접 열어 눈으로 확인한다. 빌드가 통과한 것과 화면이 맞는 것은 다르다.
- 서버 라우트는 **60초** 안에 끝나야 한다(Vercel 함수 상한). 모델 호출이 들어가는 라우트는
  시간을 재고, 넘치면 effort를 낮추거나 프롬프트에서 길이를 제한한다.
- 내 기록(localStorage)에 테스트 데이터를 넣었으면 확인이 끝나고 지운다.

## 배포

`main`에 push하면 Vercel이 자동으로 프로덕션에 올린다. 따로 명령을 돌릴 필요가 없다.
배포 전체를 한 번에 하려면 `/ship`.

- 배포 주소: https://recommend-ai-six.vercel.app
- 확인: `npx vercel ls recommend-ai` 로 Ready인지, 그다음 실제 주소를 열어본다.
- 자동 요청을 짧은 시간에 많이 보내면 Vercel 자동 방어가 켜져 403이 난다. 확인은 몇 번만 한다.
